<?xml version="1.0" encoding="utf-8"?>
<!-- The walking skeleton transfers over plain HTTP on the local network.
     TLS with certificate pinning replaces this in the pairing iteration. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
